{
	var y : Integer;

    // Unknown variable x
	if x == 0 then {
	    var x : Integer;
		x = 4; y = 5;
	} end;

	var b : Boolean;
	var x : Integer;

    //Wrong assignment
	x = if read(b) then 1 else 'c' end;

    //Invalid assignment type
	if x == 'c' then {
		var b : Boolean;
		read (b);

		//Wrong expression type
		if b then print('y', 'e', 's') else 4+6 end;
	} end;
};